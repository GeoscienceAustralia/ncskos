#!/bin/bash
# Shell script to invoke ncskosdump Python script in BASH
# Written by Alex Ip 4/10/2016
# Example invocation: ncskosdump -hs ~/workspace/ncskosdump/ncskosdump/test/sst.ltm.2000_skos.nc --skos lang=pl altLabels=True narrower=True broader=True

python -m ncskos.ncskosdump $@
